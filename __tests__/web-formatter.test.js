'use strict';

const webFormatter = require('../web-formatter.js');

describe('`write`', () => {
  it('should be good', () => {
    webFormatter.write;
    expect(true).toBeTruthy();
  });
});

describe('`append`', () => {
  it('should be good', () => {
    webFormatter.append;
    expect(true).toBeTruthy();
  });
});

describe('`tag`', () => {
  it('should be good', () => {
    webFormatter.tag;
    expect(true).toBeTruthy();
  });
});
